# 2026-07-07 研究ノート

## 作業概要

7/2のゼミで固定傾きモデルのような「トレンドが柔軟に変化しないモデル」も検討するとよい、という助言を受けた。これを踏まえ、状態空間モデル内でトレンドの硬さを変えた仕様を比較した。

今回の分析は、主仕様を直ちに置き換えるためのものではなく、主分析SSMの頑健性確認として実施した。初回実装では予測評価には進まず、モデリング比較、係数比較、分解図、残差診断に限定した。

理論的な整理は `docs/trend_rigidity_theory_note.md` にまとめ、実装は `notebooks/07_trend_rigidity_sensitivity.ipynb` で行った。

## ブランチ

- `feature/trend-rigidity-comparison`

直近の関連コミット:

- `44701295 Add trend rigidity theory note`
- `bf0932e0 Add trend rigidity sensitivity analysis`

## 比較仕様

今回比較した仕様は、`TR` = trend rigidity の略として整理した。既存の `M1`〜`M5` のようなCOVID期間感度分析とは別系統の仕様である。

### TR0_local_linear

`TR0_local_linear` は、既存の `baseline_m4` に対応する仕様である。

- local linear trend
- 水準ノイズあり
- 傾きノイズあり
- 既存の主分析SSMに相当
- 3仕様の中では最も柔らかいトレンド仕様

概念的には、トレンド水準と傾きがともに確率的に変化できる。

### TR1_fixed_slope

`TR1_fixed_slope` は、既存の `fixed_slope_m4` に対応する仕様である。

- 傾き状態は残す
- slope disturbance を selection matrix から外す
- 傾きノイズを0固定
- 水準ノイズは残す
- TR0より硬いトレンド仕様

既存の固定傾きSSMと同じ定義で実装した。

### TR2_deterministic_linear

`TR2_deterministic_linear` は、新規に実装した決定的線形トレンド仕様である。

- `trend = intercept + slope * time_index_centered`
- 水準ノイズなし
- 傾きノイズなし
- 「決定的線形トレンド回帰 + 季節状態成分 + 外生ダミー」として実装
- 完全な直線トレンドなので、TR0/TR1よりかなり硬い比較仕様

TR2はTR0/TR1と状態構造が異なる。そのためAIC/BICは報告するが、それだけで主仕様を判断しない。特にTR2は、トレンドを極端に硬くした場合の比較仕様として位置づける。

## 出力ファイル

今回新規作成した出力は以下である。

- `output/sensitivity/trend_rigidity_model_comparison.csv`
- `output/sensitivity/trend_rigidity_coefficients.csv`
- `output/sensitivity/trend_rigidity_diagnostics.csv`
- `output/sensitivity/trend_rigidity_fit_summary.csv`
- `output/sensitivity/trend_rigidity_decomposition_main.png`
- `output/sensitivity/trend_rigidity_decomposition_seasonal.png`
- `output/sensitivity/trend_rigidity_decomposition_exog.png`
- `output/sensitivity/trend_rigidity_decomposition_residuals.png`
- `output/sensitivity/trend_rigidity_trends_zoom.png`

既存の固定傾き分析の出力や、予測評価の総合比較表は上書きしていない。

## AIC/BIC/log likelihood の結果

`output/sensitivity/trend_rigidity_model_comparison.csv` に基づく結果は以下である。

| spec | LLF | AIC | BIC | convergence |
|---|---:|---:|---:|---|
| `TR0_local_linear` | 584.07 | -1124.14 | -1043.63 | converged |
| `TR1_fixed_slope` | 584.07 | -1126.14 | -1049.29 | converged |
| `TR2_deterministic_linear` | 531.00 | -1022.01 | -948.82 | not converged, warnflag=2 |

TR0とTR1は対数尤度がほぼ同等である。TR1は傾きノイズを推定しない分、パラメータが少なく、AIC/BICではTR0よりわずかに良い。

一方、TR2は未収束で、AIC/BICも大きく悪化した。完全な決定的線形トレンドは、本データには硬すぎる可能性が高い。ただし、TR2は状態構造がTR0/TR1と異なるため、AIC/BICの単純比較には注意が必要である。

## 主要イベント係数の変化

`output/sensitivity/trend_rigidity_coefficients.csv` に基づく主要イベント係数は以下である。

| spec | term | estimate | p-value | effect percent |
|---|---|---:|---:|---:|
| `TR0_local_linear` | `covid_main_effect` | 0.030 | 0.102 | 3.09% |
| `TR1_fixed_slope` | `covid_main_effect` | 0.030 | 0.107 | 3.07% |
| `TR2_deterministic_linear` | `covid_main_effect` | 0.067 | 0.000 | 6.94% |
| `TR0_local_linear` | `covid_wave1_effect` | 0.051 | 0.004 | 5.25% |
| `TR1_fixed_slope` | `covid_wave1_effect` | 0.052 | 0.004 | 5.30% |
| `TR2_deterministic_linear` | `covid_wave1_effect` | 0.063 | 0.169 | 6.51% |
| `TR0_local_linear` | `post_stat_change_effect` | -0.045 | 0.066 | -4.43% |
| `TR1_fixed_slope` | `post_stat_change_effect` | -0.045 | 0.063 | -4.42% |
| `TR2_deterministic_linear` | `post_stat_change_effect` | -0.031 | 0.000 | -3.08% |

TR0/TR1はほぼ同じである。`covid_main` は約 0.030、`covid_wave1` は約 0.051、`post_stat_change` は約 -0.045 で安定している。これは、傾きノイズを0固定しても主要結論が大きく変わらないことを示している。

一方、TR2では係数が動いた。`covid_main` は約 0.067、`post_stat_change` は約 -0.031 となり、`hike_dummy` も正方向に変化した。ただしTR2は未収束であるため、p値や有意性は慎重に扱う。TR2の係数は参考値として見るのが安全である。

## 分解図・トレンド図からの示唆

分解図と `trend_rigidity_trends_zoom.png` から、以下を確認した。

- TR0とTR1のトレンドはほぼ重なる。
- これは既存の「傾きノイズがほぼ0」という結果と整合的である。
- TR2は完全な直線トレンドであり、COVID期以降の曲がりを吸収できない。
- そのため、イベント効果、季節成分、残差に負担が移っているように見える。
- `trend_rigidity_trends_zoom.png` は、COVID期以降のトレンド差を確認する補助図として有用である。

TR0/TR1がほぼ重なることは、既存の主分析SSMが、実質的には固定傾きに近い挙動をしていた可能性を補強する。一方で、TR2の直線トレンドは明確に別の挙動を示しており、データの曲がりを十分に吸収できていない。

## 残差診断

`output/sensitivity/trend_rigidity_diagnostics.csv` に基づく残差診断は以下である。

| spec | Ljung-Box p lag12 | Ljung-Box p lag24 | residual std |
|---|---:|---:|---:|
| `TR0_local_linear` | 2.19e-07 | 1.08e-05 | 0.0107 |
| `TR1_fixed_slope` | 2.19e-07 | 1.08e-05 | 0.0107 |
| `TR2_deterministic_linear` | 6.79e-72 | 2.69e-71 | 0.0271 |

TR0/TR1のLjung-Box p値は小さく、残差自己相関は残っている。これは既存分析と同様である。

TR2はさらに悪く、lag12/lag24とも極めて小さいp値となった。残差標準偏差もTR0/TR1より大きい。したがって、トレンドの硬さを変えるだけでは、残差自己相関の課題は解消しない。残差診断上の注意は引き続き必要である。

## 現時点の判断

現時点では、以下のように整理できる。

- `TR1_fixed_slope` はTR0とほぼ同等で、AIC/BICではわずかに良い。
- 固定傾きSSMは、有力な主仕様候補または頑健性確認仕様である。
- `TR2_deterministic_linear` は、硬すぎる比較仕様として有用である。
- ただしTR2は未収束、AIC/BIC悪化、残差悪化があるため、主仕様候補ではない。
- 本データでは、完全な決定的線形トレンドよりも、固定傾きSSM程度の硬さが現実的である。

ただし、主仕様をTR1へ変更するかは、本文構成、既存結果、予測評価との整合性を踏まえて後で判断する。ここでは「TR1が主仕様に確定」とはしない。

## 研究上の解釈

柔らかいlocal linear trendと固定傾きtrendでは、主要結果がほぼ変わらなかった。これは、既存SSMの傾きノイズが実質的にほぼ0であったことと整合的である。

したがって、主分析のCOVID効果や `post_stat_change` の解釈は、傾きノイズの有無には大きく依存していないと考えられる。

一方、完全な直線トレンドまで硬くすると、モデルは不安定になり、係数や残差が悪化した。つまり、「トレンドを硬くすればよい」という単純な話ではない。データに対して、一定の柔軟性を残す必要がある。

今回の結果からは、TR0とTR1の間では主な結論が安定している一方、TR2まで硬くするとモデルとして無理が出る、という整理ができる。

## 注意点

- TR2は未収束であるため、係数のp値や有意性は参考扱いにする。
- AIC/BICは報告するが、TR2は状態構造が異なるため、単純な優劣判断には使わない。
- 残差自己相関はTR0/TR1でも残るため、HAC対応や残差診断上の注意は引き続き必要である。
- 今回は予測評価ではなく、モデリング比較である。
- 総合比較表にはまだ混ぜない。

## 残る課題・次の分析

まずは、この結果を研究ノート・補助分析として位置づける。総合比較表にはすぐ混ぜない。

次に検討する候補は以下である。

- fixed B / COVID one-year でのトレンド硬さ別予測比較
- `TR3_local_level` の追加
- SARIMA差分モデルとの補助的比較
- 主仕様をTR0のままにするか、TR1へ寄せるかの判断メモ作成

特に、TR2を予測比較に入れるかは慎重に判断する必要がある。未収束モデルを予測評価へ含める妥当性を検討したうえで、必要であればTR0/TR1中心の予測比較、またはTR2を参考線として扱う予測比較に分けるのがよい。
